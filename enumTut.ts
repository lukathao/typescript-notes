//enums are easy ways to define sets of named constants
enum LoginError {
    Unauthorized = "unautheorized",
    NoUser = "user does not exist",
    WrongCredentials = "wrong credentials",
    Internal = "internal",
} 


